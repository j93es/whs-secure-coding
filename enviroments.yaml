name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - flask
      - flask-socketio
      - flask-sqlalchemy
      - flask_wtf
      - flask_limiter
      - python-dotenv
      - gunicorn
      - bcrypt
      - Flask-JWT-Extended
      - PyJWT
      - eventlet
